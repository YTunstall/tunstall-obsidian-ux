Proportion is the relationship between the sizes of design elements. Using the right sizes helps show importance, creates visual balance, and makes the interface easier to understand. It works closely together with the design principle, hierarchy.

>**Example**: Making the main heading larger than the body text so users immediately recognize the most important information.